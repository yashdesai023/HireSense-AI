import React from 'react';
import RegisterForm from '../../components/register-login/register';

const RegisterPage: React.FC = () => {
  return (
    <div>
      {/* You can add layout or container elements here if needed */}
      <RegisterForm />
    </div>
  );
};

export default RegisterPage;